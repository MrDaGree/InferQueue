# cgroup.conf — devcontainer
# With privileged: true the container has access to /sys/fs/cgroup.
# Let Slurm autodetect v1 vs v2 based on what the kernel exposes.
CgroupPlugin=autodetect
CgroupAutomount=yes